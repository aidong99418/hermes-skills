# VPO: 多样性驱动的策略优化

> 核心来源: arXiv 2605.22817 | 2026-05-21 | Bahlous-Boldi et al.

## 核心发现

### 问题：标准后训练=单目标优化，压制了多样性

LLM后训练通常优化单一标量奖励函数（BLEU / Perplexity / 人类偏好），导致输出分布熵极低——模型只会"最安全"的答案，遇到推理时搜索需要多样化时就傻眼了。

```
标准RL: reward = f(single_metric)  → 输出趋同
VPO:   reward = f(vector_rewards)  → 保留多样性
```

### VPO核心：显式训练策略去适应多样化奖励

不追求单峰最优，而是训练策略在**未知奖励向量**上也能泛化。

**关键思想：** 推理时搜索（如AlphaEvolve）需要策略在多种奖励信号下都表现出色，训练时就该暴露这种多样性。

```python
# 向量奖励训练
def vpo_loss(policy, reward_vector):
    # 不优化单一目标，而是优化策略对多样化奖励的适应性
    expected_diversity = E[policy.sample() | reward_vector]
    return -diversity_bonus + baseline_cross_entropy
```

## 对机器猫系统的启发

| VPO发现 | 机器猫现状 | 改进方向 |
|---------|---------|---------|
| 训练多样性 > 单目标最优 | 任务路由只看命中率 | 考虑路由策略对边界情况的容忍 |
| 未知奖励函数下也能工作 | 模型调优只盯准确率 | 模型除了准，还要"多面手" |
| 推理时搜索需要多样性储备 | 推理靠单一模型硬算 | 备选模型池本质是多样性 |

## 核心原则

1. **能力边界比单一指标重要**——能处理多种情况 > 某一项满分
2. **多样性是防御性储备**——平时用主流，边界情况靠多样性救场
3. **模型池就是多样性的实现**——多模型备选 = VPO思想的工程化

---
*提炼自Hermes学习系统 | 2026-05-23*

# Cron任务静默模式

## 核心原则

> **用户不需要知道系统正常运行，只有出问题时才打扰。**

## 三种交付模式

| 模式 | deliver参数 | 行为 |
|------|------------|------|
| 静默 | `local` | 只写本地文件，不推送 |
| 通知 | `origin` 或 `weixin` | 推送到微信 |
| 自用 | 无（cron内部使用） | 不交付 |

## 适合静默的任务

- 日志分析/归档
- 缓存清理
- 健康检查（频繁）
- 自动学习/抓取
- 备份（频繁）
- 模型warmup

## 紧急情况才通知

以下情况才切换回 weixin 通知：
1. 连续3次cron执行失败
2. 系统磁盘接近满
3. Ollama服务彻底宕机
4. 备份推送失败

## 静默自愈原则

```
cron任务失败 → 自动重试3次 → 仍失败才通知
```

## 实现方式

```python
result = {
    "status": "success",  # 或 "failed"
    "message": "...",
    "files_changed": ["..."]
}
# 只在failed时推weixin
```
